# Convert Python Program to Windows Program

Install [PyInstaller](https://pyinstaller.org/en/stable/)

```bash
pip install pyinstaller
```

Convert into .exe

```bash
pyinstaller main.py --onefile -w
```
